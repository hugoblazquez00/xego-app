import React from "react"

export function DatabaseView({ currentScreen }) {
  return (
    <div className="database-view">
      {/* Contenido de DatabaseView será añadido posteriormente */}
    </div>
  )
} 